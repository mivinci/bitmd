{
  "name": "bitmd",
  "description": "The monorepo of bitmd.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:bitmd": "cd packages/bitmd && bun run build",
    "build:plugin-gfm": "cd packages/plugin-gfm && bun run build",
    "build:plugin-katex": "cd packages/plugin-katex && bun run build",
    "build:plugin-highlight": "cd packages/plugin-highlight && bun run build",
    "build:plugin-image": "cd packages/plugin-image && bun run build"
  }
}